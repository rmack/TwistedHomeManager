plugins {
    id "com.android.application"
}

android {
    namespace = "com.twsitedapps.homemanager"
    compileSdk = 35

    defaultConfig {
        applicationId = "com.twsitedapps.homemanager"
        minSdk = 23
        targetSdk = 35
        versionCode = 19
        versionName = "1.0.1.9"
    }

    sourceSets {
        main {
            manifest.srcFile "../AndroidManifest.xml"
            java.srcDirs = [ "../src" ]
            res.srcDirs = [ "../res" ]
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
